import React from 'react';
import { useNavigate } from 'react-router-dom';
import { routines_create } from './../../api/routines';

const Routine = () => {
    const nav = useNavigate();

    const [list,set]
    
    return (
        <div>
            <h1>루틴 관리</h1>

            {FileList.}

            <button>
                나만의 루틴 만들기
            </button>
        </div>
    );
};

export default Routine;